import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

    logoContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        marginTop: -150,
        marginBottom: 40,
    },
    logoImage: {
        width: 360,
        height: 200,
        resizeMode: 'contain',

    }
});
