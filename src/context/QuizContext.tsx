import React, { createContext, useContext, useState, ReactNode } from 'react';

export interface Question {
}

export interface Score {
}

interface QuizContextType {
};
// arquivo criado para a pessoa que vai criar o context do quiz